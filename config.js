// You can use config.dev.js to override config options

module.exports = {
    // Bot token
    token: ``,
    
    // Bot status ('online', 'idle', 'dnd')
    status: 'idle',
    
    // Bot "Now Playing" text
    statusMsg: 'SlotBot is currently experiencing some downtime! DM me ~slothub to get updated on my status!',
    
    // Prefix for DM commands
    prefix: '~',
    
    // Text to show when the user runs `~slothub` in DMs
    infoText: `Here's an invite for SlotBot's support guild: https://discord.gg/ESkzfwf`,
    
    // Text to show when an unknown command is ran. Set to false to disable.
    notFoundText: false,
}
